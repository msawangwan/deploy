#!/bin/bash

# bootstrap sources all files in lib/
#

echo "bootstrap"; echo

for s in lib/*; do
    RES="$(source $s)"
    #RES="$(. $s)"
    if [ "$RES" != 0 ]; then
        echo "err: failed to load: $s"
    else
        echo "loaded: $s"
    fi
done

exit 0
