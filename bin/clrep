#!/bin/sh

if [ $# != 2 ]; then
    echo "err: missing args"
    echo "usage: `basename $0` <username> <repo>"; echo
    exit 1
fi

PREFIX="https://github.com"
SUFFIX=".git"

USERNAME="/$1"
REPO="/$2"

URL="${PREFIX}${USERNAME}${REPO}${SUFFIX}"

echo "url: ${URL}"

#git clone "${URL}"
